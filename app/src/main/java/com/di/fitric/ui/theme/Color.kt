package com.di.fitric.ui.theme

import androidx.compose.ui.graphics.Color

// App-wide Dark Theme Palette
val AppDarkBackground = Color(0xFF101010) // A near-black for maximum contrast
val AppDarkSurface = Color(0xFF1E1E1E)   // A slightly lighter dark grey for cards
val AppTextPrimary = Color(0xFFFFFFFF)    // Pure white for primary text (e.g., timer)
val AppTextSecondary = Color(0xFFB0B0B0) // A light grey for less important text

// Action Colors based on function
val ActionGreen = Color(0xFF4CAF50)      // A clear green for the START button
val ActionRed = Color(0xFFE53935)        // A clear red for the STOP button
val ActionBlue = Color(0xFF1E88E5)       // A friendly blue for the TRAINER button